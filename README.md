# for private page - use _Components
# want to access _components -- then use %5F instead of _

# Lec - sixteen
# where the html of our app can be generated? - Server,client
# 1.SSR (Server Site Rendering)
  # i. static site generatiion(SSG)
  # ii. incremental static regeneration(ISR)
# 2.CSR as react rendering (client site rendering)